# Supplied historical snapshots

`v0.1/` and `v0.2/` preserve the source scaffolds supplied for this revision. They are reference snapshots, not installed packages, current commands or approved license notices. The active v0.3 code is in the repository's top-level `src/` directory.

The original release ZIP hashes are recorded in `input-checksums.json`. Generated caches may be omitted from the snapshots; the source is retained. Known defects are documented in [the version review](../docs/version-review.md). Do not run historical CLIs against your current output folders.

The owner selected MIT for the v0.3 release, matching TurtleML. Historical Apache/CC placeholder suggestions in these snapshots describe earlier scaffolding and do not define the active release's terms; see the root LICENSE.

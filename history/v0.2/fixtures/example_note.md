# Demonstration note

This is a harmless local fixture. Its value is not the prose; it is that the
reference pipeline can preserve identity, create a readable view, and emit a
machine-readable packet without pretending the output is canonical truth.
